Необходимо реализовать апи метод, который на вход получает два ip-адреса (параметры From и To) и
 1. Выдает обратно все ip-адреса, лежащие между ними
 2. Записывает результат в базу данных, если запроса с такими параметрами еще не было.
 3. Отдает результат из базы, если запрос с такими параметрами уже был
технологии:
c#, .net core 3.1 либо .net 5
БД ms sql либо postgresql